# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# SearXNG config thật (chứa secret_key / local overrides) — dùng bản mẫu *.example.yml
searxng/settings.yml
# Backup file thật cũng chứa secret (hướng dẫn nâng cấp dùng settings.yml.bak)
searxng/settings.yml.*

# Aurora ChatGPT Web credentials — never commit.
aurora/session_tokens.txt
aurora/refresh_tokens.txt
aurora/access_tokens.txt

# IDE / OS
.idea/
.vscode/
.DS_Store
